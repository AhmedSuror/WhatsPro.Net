# Configuration

The SDK is configured via `WhatsProOptions`:
- `BaseUrl`: The root API endpoint.
- `Email` & `Password`: Used to fetch the JWT token transparently.
- `EncryptionKey`: Overrides the default AES key.
- `Timeout`: Adjust HTTP timeouts.
